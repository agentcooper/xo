{
  "name": "xo-game",
  "version": "0.0.1",
  "description": "Simple XO game for two players",
  "main": "src/index.js",
  "scripts": {
    "start": "open index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist; browserify src/index.js -t babelify -o dist/bundle.js",
    "watch": "watchify src/index.js -t babelify -o dist/bundle.js --verbose"
  },
  "author": "Artem Tyurin <artem.tyurin@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "watchify": "^3.7.0"
  }
}
